id: require-otterdog-repo
name: blabla
description: |-
  sdfsdfssdsdf.
type: append_configuration
config:
  condition: >-
    $count(
      $.repositories[name = ".otterdog"]
    ) = 0
  content: |-
    {
      # snippet added due to '{{blueprint_url}}'
      _repositories+:: [
        orgs.newRepo('.otterdog')
      ],
    }
  reviewers:
    - "{{project_name}}-project-leads"
